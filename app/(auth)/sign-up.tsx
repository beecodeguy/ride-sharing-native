import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SignupRoute = () => {
  return (
    <SafeAreaView>
      <Text>SignupRoute</Text>
    </SafeAreaView>
  );
};

export default SignupRoute;
