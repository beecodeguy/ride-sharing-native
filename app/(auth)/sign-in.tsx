import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SignInRoute = () => {
  return (
    <SafeAreaView>
      <Text>SignInRoute</Text>
    </SafeAreaView>
  );
};

export default SignInRoute;
