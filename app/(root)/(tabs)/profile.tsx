import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const ProfileRoute = () => {
  return (
    <SafeAreaView>
      <Text>ProfileRoute</Text>
    </SafeAreaView>
  );
};

export default ProfileRoute;
