import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const RidesRoute = () => {
  return (
    <SafeAreaView>
      <Text>RidesRoute</Text>
    </SafeAreaView>
  );
};

export default RidesRoute;
